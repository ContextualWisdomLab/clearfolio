보안: AdminController에 대한 인증 및 권한 부여 추가

- 관리자 엔드포인트(`getAllJobs`, `deleteJob`, `retryDeadLettered`)에 `TenantAccessService`를 주입하여 인증 및 권한 검증 로직 추가
- `TenantPermissions.java`에 `ADMIN_READ`, `ADMIN_WRITE` 권한 상수를 추가
- 테스트 클래스(`AdminControllerTest.java`)에서 모의 객체(`TenantAccessService`)를 주입하고 적절한 헤더를 전송하여 변경된 권한 검증 로직을 커버
- Sentinel 저널 파일 업데이트를 통해 인증 누락 취약점에 대한 보안 인사이트 기록
