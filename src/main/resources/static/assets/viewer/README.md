# Viewer Assets

This directory contains static assets for the Clearfolio document viewer.

These files are plain HTML, CSS, and vanilla JavaScript intended to be served directly to the browser by the Spring Boot application. They are not Node.js source files, and there is no Node.js build process, package.json, or frontend testing framework associated with them.
